[project]
name = "invoice-vault"
description = "Fractional, yield-bearing escrow that turns pending developer grants into instant liquidity"
authors = []
telemetry = false
cache_dir = "./.cache"
requirements = []

[contracts.invoice-vault]
path = "contracts/invoice-vault.clar"
clarity_version = 2
epoch = 2.4

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
